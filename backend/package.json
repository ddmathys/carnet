{
  "name": "bloom-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Backend proxy Bloom — IA (DeepSeek) et emails (Resend) avec authentification Firebase",
  "dependencies": {
    "firebase-admin": "^13.0.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@vercel/node": "^5.0.0",
    "typescript": "^5"
  }
}
